import {FC} from 'react';

const About: FC = () => {
  return (
    <section id="about" className="bg-cover bg-center h-screen">
      <div className="flex items-center justify-center h-full bg-black bg-opacity-50">
        About
      </div>
    </section>
  );
};

export default About;
