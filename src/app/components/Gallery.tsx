import {FC} from 'react';

const Gallery: FC = () => {
  return (
    <section id="gallery" className="bg-cover bg-center h-screen" >
      <div className="flex items-center justify-center h-full bg-black bg-opacity-50">
        Gallery
      </div>
    </section>
  );
};

export default Gallery;
