import {FC} from 'react';

const Career: FC = () => {
  return (
    <section id="career" className="bg-cover bg-center h-screen">
      <div className="flex items-center justify-center h-full bg-black bg-opacity-50">
        Career
      </div>
    </section>
  );
};

export default Career;
