import {FC} from 'react';

const Contacts: FC = () => {
  return (
    <section id="contacts" className="bg-cover bg-center h-screen">
      <div className="flex items-center justify-center h-full bg-black bg-opacity-50">
        Contacts
      </div>
    </section>
  );
};

export default Contacts;
